let saludos = {
    hola : 
    function (nombre) {
        return `hola ${nombre} como estas?`
    },
    chau : 
    function (nombre) {
        return `chau ${nombre} que te valla bien`
    }
}

module.exports = saludos;
